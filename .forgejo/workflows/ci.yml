name: Test CI

on:
  workflow_dispatch:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  test:
    runs-on: act-latest
    env:
      EXTENSION_ARTIFACT_NAME: llm_companion
    steps:
      - uses: https://github.com/actions/checkout@v6
      - uses: https://github.com/pnpm/action-setup@v4
        with:
          version: 10
      - uses: https://github.com/actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm test
      - name: Renovate
        continue-on-error: true
        uses: https://github.com/renovatebot/github-action@v46.1.4
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          mount-docker-socket: false
        env:
          RENOVATE_AUTODISCOVER: false
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_PLATFORM: forgejo
          RENOVATE_ENDPOINT: https://forgejo.o-st.dev/api/v1
          RENOVATE_BINARY_SOURCE: install
          GIT_AUTHOR_NAME: Renovate Bot
          GIT_AUTHOR_EMAIL: renovate-bot@o-st.dev
          GIT_COMMITTER_NAME: Renovate Bot
          GIT_COMMITTER_EMAIL: renovate-bot@o-st.dev
          RENOVATE_GIT_AUTHOR: Renovate Bot <renovate-bot@o-st.dev>
          RENOVATE_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-prerelease:
    if: ${{ github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/') }}
    needs: test
    runs-on: act-latest
    permissions:
      contents: write
    env:
      EXTENSION_ARTIFACT_NAME: llm_companion
    steps:
      - uses: https://github.com/actions/checkout@v6
      - uses: https://github.com/pnpm/action-setup@v4
        with:
          version: 10
      - uses: https://github.com/actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install
      - name: Set numeric package version
        run: pnpm pkg set version=0.0.0
      - run: pnpm build
      - run: pnpm build:firefox
      - run: WXT_RELEASE_VERSION=latest pnpm run zip
      - run: WXT_RELEASE_VERSION=latest pnpm run zip:firefox
      - name: Copy artifacts
        run: |
          mkdir -p /upload
          mv .output/*.zip /upload
      - uses: https://github.com/actions/forgejo-release@v2.11.1
        with:
          direction: upload
          url: https://forgejo.o-st.dev
          repo: ST_Consultancy/Llm_companion
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: latest
          release-dir: /upload
          prerelease: true
          release-notes: Development build from CI
          override: true
